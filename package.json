{
  "name": "calling-fortran-from-node",
  "version": "0.0.0",
  "description": "Calling Fortran from within Node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp rebuild"
  },
  "author": "Dane Petersen",
  "license": "MIT",
  "gypfile": true,
  "dependencies": {
    "node-gyp": "^0.13.0"
  }
}
